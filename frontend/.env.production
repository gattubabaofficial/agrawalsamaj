# Dedicated backend origin for resolving media / uploaded images (cover images, banners, receipts, etc.)
# JSON API requests intentionally remain relative (/api/v1) and proxied server-side.
NEXT_PUBLIC_MEDIA_URL=https://api.agrasamaj.in
